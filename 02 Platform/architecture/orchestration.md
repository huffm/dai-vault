# orchestration

The platform should orchestrate reusable agents and tools without embedding niche-specific rules into the core execution path.

The shared system should coordinate:
- input collection
- evaluation
- synthesis
- compliance checks
- delivery formatting
